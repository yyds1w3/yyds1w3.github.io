---
// src/components/ProblemCard.astro
interface Props {
  platform: string;
  rating?: number; // 可选属性，有的题目没分
  tags?: string[]; // 可选
  link: string;
}

// 从 Astro.props 中解构拿到传入的数据
const { platform, rating, tags = [], link } = Astro.props;

// 可以在这里写一些简单的逻辑
const difficultyColor = rating && rating >= 2000 ? 'text-red-500' : 'text-yellow-600';
---

<div class="my-6 p-4 border-2 border-gray-200 rounded-lg bg-gray-50 dark:bg-gray-800 dark:border-gray-700">
  <div class="flex flex-wrap items-center gap-4 mb-3 text-sm font-medium">
    <span class="flex items-center px-2.5 py-0.5 rounded-full bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-300">
      🏷️ {platform}
    </span>
    
    {rating && (
      <span class={`flex items-center ${difficultyColor}`}>
        ⭐ Rating: {rating}
      </span>
    )}

    {tags.length > 0 && (
      <div class="flex flex-wrap gap-2 text-gray-600 dark:text-gray-400">
        {tags.map((tag) => (
          <span>#{tag}</span>
        ))}
      </div>
    )}
  </div>

  <div>
    <a href={link} target="_blank" rel="noopener noreferrer" class="text-blue-600 hover:underline dark:text-blue-400 flex items-center font-semibold">
      🔗 题目链接：点击跳转
    </a>
  </div>
</div>


<style>
  .problem-card {
    border: 2px solid #e5e7eb;
    padding: 1rem;
    border-radius: 0.5rem;
  }
</style>